"""Observation normalization placeholder."""

import numpy as np
from numpy.typing import NDArray


def normalize_observation(observation: NDArray[np.float64]) -> NDArray[np.float64]:
    """Normalize an observation using a future schema."""

    raise NotImplementedError("Observation normalization is not implemented")
